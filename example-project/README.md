# Example Project

This directory is the minimal fixture used by cc-supervisor integration tests.

Files are intentionally small so Claude Code can finish a task quickly without
external dependencies.
